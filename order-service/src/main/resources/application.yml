spring:
  application:
    name: order-service
  profiles:
    default: static-discovery

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

