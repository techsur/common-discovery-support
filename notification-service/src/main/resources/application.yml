spring:
  application:
    name: notification-service
  profiles:
    default: static-discovery

server:
  port: 8083

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

