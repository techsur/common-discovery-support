spring:
  application:
    name: inventory-service
  profiles:
    default: static-discovery

server:
  port: 8082

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

